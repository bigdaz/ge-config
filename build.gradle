plugins {
  id "net.researchgate.release" version "2.8.1"
}

task build {
    doFirst {
        println "BUILDING..."
    }
}

release {
    failOnPublishNeeded = false
    tagTemplate = 'ge-connect-${version}'
    git {
        requireBranch = "release"
        pushOptions = ['-n']
    }
}
